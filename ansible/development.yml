---
- name: Deploys the whole software stack for a CakePHP application
  hosts: development
  user: vagrant
  sudo: yes

  roles:
    - system
    - php
    - nginx
    - percona
    - postgresql
    - redis
    - memcached
